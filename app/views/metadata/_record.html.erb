<table class="metadata">
  <tbody>
  <% metadata_record.fields.each do |f| %>
    <% partial = f.respond_to?(:links) ? 'metadata/link_field' : 'metadata/text_field' %>
    <%= render(partial: partial, locals: {field: f}) %>
  <% end %>
  </tbody>
</table>
